{
  "name": "nexus-root",
  "private": true,
  "scripts": {
    "build": "npm install -g pnpm && cd nexus/providers && pnpm install && pnpm run build && cd .. && pnpm install && pnpm run build && cd .. && rm -rf dist && rm -rf public && mv nexus/dist dist && cp -r dist public"
  }
}
