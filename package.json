{
  "name": "nexus-root",
  "private": true,
  "scripts": {
    "build": "cd nexus && npm install && npm run build && cd .. && rm -rf dist && rm -rf public && mv nexus/dist dist && cp -r dist public"
  }
}
