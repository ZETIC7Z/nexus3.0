name: Deploy

on:
  push:
    branches:
      - production
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.14.4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          known_hosts: unnecessary

      - name: Add known hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Clone private providers
        run: |
          git clone --depth 1 https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/xp-technologies-dev/private-providers.git /tmp/private-providers
          ls /tmp/private-providers/lib/
      - name: Force private providers
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.dependencies['@nexus/providers'] = 'file:/tmp/private-providers';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
          grep providers package.json
          rm -f pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --force

      - name: Build
        run: pnpm build
        env:
          VITE_TMDB_READ_API_KEY: ${{ secrets.VITE_TMDB_READ_API_KEY }}
          VITE_SHOW_SUPPORT_BAR: "true"
          VITE_ALLOW_FEBBOX_KEY: "true"
          VITE_TRAKT_CLIENT_ID: ${{ secrets.VITE_TRAKT_CLIENT_ID }}
          VITE_TRAKT_CLIENT_SECRET: ${{ secrets.VITE_TRAKT_CLIENT_SECRET }}
          VITE_TRAKT_REDIRECT_URI: ${{ secrets.VITE_TRAKT_REDIRECT_URI }}
          VITE_BACKEND_URL: ${{ secrets.VITE_BACKEND_URL }}
          VITE_ENABLE_HOME_AD: "true"
          VITE_HOME_AD_ZONE_ID: "11283226"
          VITE_ENABLE_SECONDARY_AD: "false"
          VITE_SECONDARY_AD_ZONE_ID: "11283340"
          VITE_ENABLE_BOOKMARKS_AD: "true"
          VITE_PRIMARY_BANNER_GIF: "true"
          VITE_PRIMARY_BANNER_GIF_LINK: "https://i.ibb.co/Mk3BfjG2/undefined-Imgur-ezgif-com-optimize-1.gif"
          VITE_PRIMARY_BANNER_GIF_URL: "https://clixou.com/"
          VITE_BOOKMARKS_AD_ZONE_ID: "11283340"
          VITE_ENABLE_POPUNDER: "true"
          VITE_POPUNDER_SCRIPT_URL: "https://pimplehardnesscarnivorous.com/a5/fb/2b/a5fb2bb0f691b3383e76e931f20def0a.js"
          VITE_POPUNDER_COOLDOWN_HOURS: "1"
          VITE_ENABLE_SOCIAL_BAR: "true"
          VITE_SOCIAL_BAR_SCRIPT_URL: "https://pimplehardnesscarnivorous.com/c6/0a/23/c60a23316a7972924bf2a93052a57795.js"
          VITE_ENABLE_SMARTLINK: "true"
          VITE_SMARTLINK_URL: "https://pimplehardnesscarnivorous.com/h7h60krf?key=f96df9f12020c7164c7ba839d83df3f6"
          VITE_ENABLE_PLAYER_BANNER: "false"
          VITE_PLAYER_BANNER_ZONE_ID: ""
          VITE_ENABLE_MONETAG_ONCLICK: "true"
          VITE_MONETAG_ONCLICK_URL: ""
          VITE_ENABLE_MONETAG_INPAGE: "true"
          VITE_MONETAG_INPAGE_URL: ""
          VITE_ENABLE_MONETAG_VIGNETTE: "true"
          VITE_MONETAG_VIGNETTE_URL: ""
          VITE_ENABLE_MONETAG_MULTITAG: "true"
          VITE_MONETAG_MULTITAG_URL: "https://quge5.com/88/tag.min.js"
          VITE_MONETAG_MULTITAG_ZONE: "277030"
          # Monetag domain verification meta tag (empty = tag not rendered)
          VITE_MONETAG_VERIFICATION_TOKEN: ${{ secrets.VITE_MONETAG_VERIFICATION_TOKEN }}
          VITE_ENABLE_RYBBIT: "true"
          VITE_RYBBIT_SCRIPT_URL: ${{ vars.VITE_RYBBIT_SCRIPT_URL }}
          VITE_RYBBIT_SITE_ID: "de683ab9c45a"
          VITE_USE_SIMKL: "true"
          VITE_SIMKL_CLIENT_ID: "ba3e36f42507238bc3466a995763505b0cf4407953f286e09935c51bf3347ebc"
          VITE_SIMKL_CLIENT_SECRET: ${{ secrets.VITE_SIMKL_CLIENT_SECRET }}
          VITE_SIMKL_REDIRECT_URI: "https://nexus.mov/settings"
          VITE_DMCA_EMAIL: "nexus-dmca@proton.me"
      - name: Clean credentials
        if: always()
        run: rm -rf /tmp/private-providers

      - name: Deploy to VPS
        run: |
          tar -czf /tmp/dist.tar.gz -C dist .
          scp /tmp/dist.tar.gz ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/tmp/dist.tar.gz
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "
            set -e
            rm -rf /var/www/nexus.new && mkdir -p /var/www/nexus.new
            tar -xzf /tmp/dist.tar.gz -C /var/www/nexus.new
            rm -f /tmp/dist.tar.gz
            rm -rf /var/www/nexus.old
            mv /var/www/nexus /var/www/nexus.old
            mv /var/www/nexus.new /var/www/nexus
            rm -rf /var/www/nexus.old
          "
